spring:
  application:
    name: micropersonnel-api-login
  boot:
    admin:
      client:
        url: http://localhost:21000
eureka:
  client:
    service-url:
      defaultZone: http://localhost:20000/eureka
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
server:
  port: 30000
#logging:
#  file:
#    path: ../cloud-logs
